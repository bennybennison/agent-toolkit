# MCP Templates Bridge

Canonical MCP template artifacts now live in `agent-toolkit`.

This vendored copy supports the standalone optional runtime inside
`agent-toolkit` while older compatibility surfaces are still being retired.
