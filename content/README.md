# Toolkit Content

This directory holds toolkit-owned legacy markdown content that is still useful
across adapter surfaces inside the standalone `agent-toolkit` package.

Current areas:
- `agents/`
- `commands/`
- `skills/`
